{
  "name": "sltiet-ums-app",
  "version": "1.0.0",
  "description": "SLTIET UMS Attendance App in Capacitor",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build script needed for static www folder'",
    "cap-init": "npx cap init",
    "cap-sync": "npx cap sync",
    "cap-copy": "npx cap copy",
    "cap-open-android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
